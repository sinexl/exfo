func main () {
  a := 0; // Declaration
  putnum(a);
  a := 10; // Shadowing
  putnum(a);
  a = a + 10; // Mutation
  putnum(a);
  putnum(65 * 2);
  
  b := 0; 
  { 
      b = 10; 
      putnum(b);
  } 
}